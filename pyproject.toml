[project]
name = "npf_renderer"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "dominate",
]

[project.optional-dependencies]
# A prettyprinter libary for debugging purposes
debug = ["prettyprinter==0.18.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = "tests"